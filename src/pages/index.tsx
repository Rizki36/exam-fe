import LandingPage from '@/components/LandingPage';
import { Page } from '@types';

const Landing: Page = () => {
  return (
    <>
      <LandingPage />
    </>
  );
};

export default Landing;
